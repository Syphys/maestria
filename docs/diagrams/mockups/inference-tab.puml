@startuml mockup-inference-tab
!include ../_includes/style.iuml

' Bonus — Salt UI mockup of the Inférence tab. Salt is PlantUML's
' built-in mock-up language. Useful when describing UX flows to
' contributors without a screenshot in git LFS.

title UI mockup — Inférence tab (Salt)

salt
{
  {/ Tags | Description | <b>Inférence</b> | Linked }
  {* Model | qwen-coder-32b.gguf [Q4_K_M] | 18 GB | ctx 32768 }
  --
  {[ ▶ Run ▼ ] | Runner: llama.cpp (ik_llama)         | ⚙ Configure }
  --
  {+ <b>Run parameters</b>           |        |       }
  {     Parameter | Estimated | Used       }
  {     ngl       |     99    |     99     }
  {     ctx       |   32768   |   32768    }
  {     threads   |     8     |      8     }
  {     batch     |    512    |    512     }
  {     flashAttn |    on     |     on     }
  {     mlock     |   false   |    false   }
  {     fit       |    ✓ on   |   ✓ on     }
  --
  {[ Advanced parameters... (3 custom) ]                     }
  --
  {<b>Compétence</b> (Beta-Laplace pass-rate)                  }
  {        radar (R5 — 16 axes)                                 }
  {      ▸ Arbre de compétence (collapsible)                    }
  {      ▸ Parler libre (freegen_text + projection)             }
  {                                                              }
  {<i>Suite tree-v0 · caractérisé 2026-05-23 19:17</i>          }
  {[ ⚗ Re-caractériser ] | [ ❓ Questions sources ]            }
}

== Bulk panel (above the Console, left sidebar) ==

salt
{
  {<b>Caractériser tous les modèles</b>                         }
  --
  { [ ] Forcer                |                                  }
  { [✓] Parler libre          | [ ▶ TOUT CARACTÉRISER ]         }
  { [ ] Sans calcul vectoriel | [ ❓ Questions sources ]        }
  --
  {<i>Modèle 12/52 — qwen-coder-32b.gguf</i>                     }
  { ▇▇▇▇▇▇▇▆▁▁▁▁▁ 60%                                            }
  --
  { ( Erreurs (3) | Logs serveur | Interactions ) | [ 📋 Copier ] }
  {<i>scrollable tab body (max ~220 px, infinite scroll,</i>     }
  {<i>polling pauses while a text selection is active)</i>      }
}

note right
  Salt is intentionally rough — it
  conveys layout / hierarchy, not
  pixel-perfect rendering. For a
  real screenshot, run the app.
end note

@enduml
